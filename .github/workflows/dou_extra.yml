name: DOE

on:
  schedule:
    - cron: "0 * * * *"   # roda de 1 em 1 hora, no minuto 0
  workflow_dispatch: {}

jobs:
  run-extra:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create credentials.json from secret
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
        run: |
          python - <<'PY'
          import os, json, pathlib
          raw = os.environ.get("GOOGLE_APPLICATION_CREDENTIALS_JSON")
          if not raw:
            raise SystemExit("⚠ Secret GOOGLE_APPLICATION_CREDENTIALS_JSON não encontrado.")
          info = json.loads(raw)
          if "private_key" in info and "\\n" in info["private_key"]:
            info["private_key"] = info["private_key"].replace("\\n", "\n")
          pathlib.Path("credentials.json").write_text(json.dumps(info), encoding="utf-8")
          print("✅ credentials.json criado")
          PY

      - name: Run DOU Extra scraper
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
          PLANILHA: ${{ secrets.PLANILHA }}                    # planilha geral
          PLANILHA_CLIENTES: ${{ secrets.PLANILHA_CLIENTES }}  # planilha com abas por cliente
          EMAIL: ${{ secrets.EMAIL }}
          DESTINATARIOS: ${{ secrets.DESTINATARIOS }}
          BREVO_API_KEY: ${{ secrets.BREVO_API_KEY }}
          # opcional: customizar seções extras a varrer (padrão: DO1E,DO2E,DO3E)
          # DOU_EXTRA_SECOES: "DO1E,DO2E"
        run: |
          python dou_extra.py

      # Alinhamento nas abas por cliente (usa coluna "Conteúdo"; pula "Giro de notícias")
      - name: Run DOU alignment (per client tabs)
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
          PLANILHA_CLIENTES: ${{ secrets.PLANILHA_CLIENTES }}
          GENAI_API_KEY: ${{ secrets.GENAI_API_KEY }}
        run: |
          python alinhamento_dou.py
